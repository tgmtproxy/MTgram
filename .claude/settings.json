{
  "respectGitignore": true,
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "jq 'if (.tool_input.file_path // \"\") | endswith(\".patch\") then {hookSpecificOutput:{hookEventName:\"PreToolUse\",permissionDecision:\"deny\",permissionDecisionReason:\"patches/ is auto-generated by stgit — edit the worktree and refresh via stgit instead (see CLAUDE.md).\"}} else empty end'"
          }
        ]
      }
    ]
  }
}